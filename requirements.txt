# Core
cachetools ~= 5.3.0
motor >= 3.3.2, < 4
nextcord [voice] ~= 2.6.0
onami ~= 2.6.3  # unmaintained
pydantic-settings >= 2.1.0, < 3
pydantic >= 2.6.0, < 3
typing_extensions >= 4.6.1, < 5

# General utils
listre ~= 1.0.0

# AI talk
a3rt-talkpy ~= 1.0.0

# MC server
mcstatus ~= 10.0.0  # required < 11

# Music
niconico-dl ~= 2.2.7  # discontinued (moved to niconico.py)

# Shiritori
jaconv ~= 0.3.4  # unmaintained
pykakasi == 2.2.1  # discontinued (archived)

# Steam server
python-a2s ~= 1.3.0  # unmaintained

# Translate
deepl ~= 1.17.0
googletrans == 4.0.0-rc1  # unmaintained
pycld2 @ git+https://github.com/abosamoor/pycld2.git@c71aa20a5cb04c93ac434abafea53bd570c9c1dc  # unmaintained

# TTS
alkana == 0.0.3  # unmaintained

# Debug
distro ~= 1.9.0
psutil ~= 5.9.0
websockets >= 12.0, < 13
